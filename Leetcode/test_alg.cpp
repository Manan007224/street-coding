#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vi;
typedef vector<vi> vvi;

vvi find(vvi ip){
	int r=ip.size(); int c=ip[0].size();
	vi curr_ptr(r,0);
	while(1){
		int mn = ip[0][curr_ptr[0]];
		if(ip[0].size() ==0 || ip[0].size() < curr_ptr[0]) return res;
		for(int x=0; x<sz; x+)
	}
}

int main(){
	vector<vector<int>> v={{1,1,2,3,8,9},{2,3,8,10,11},{2,2,3,3,6,7,7,8}};

	return 0;
}