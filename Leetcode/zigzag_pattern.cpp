#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    string convert(string s, int numRows){
    	string ans;
    	int cnt = 7;
        for(int k=0; k<s.length(); k++){
        	if(ans==)
        }
    }
};

int main(){
	return 0;
}