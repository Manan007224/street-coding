#include <bits/stdc++.h>
using namespace std;

vector<int> solution(vector<int> N, int K) {
	vector<vector<int>> arr(N.size() - K + 1);
	unordered_map<int, int> mp;
	
  return {}; 
}

int main () {
	vector<int> arr = {1,1,2,3};
	vector<int> res = solution(arr, 2);
	for(auto r: res) cout << r << endl;
	return 0;
}